/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router no longer supports the legacy i18n config. Locale handling will
  // be added later using route groups + `generateStaticParams` when needed.
};

module.exports = nextConfig;
