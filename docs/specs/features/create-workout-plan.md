# Feature: Create Workout Plan

This filename is kept for backward compatibility in the repo history.

## Canonical spec

Use the UI creation-flow spec here instead:

- `docs/specs/features/create-workout-plan-via-ui.md`

## Notes

- The current product flow for creating plans from the dashboard is the minimal UI dialog backed by `POST /api/workouts/plans`
- The API/service implementation details are additionally documented in `docs/features/create-workout-plan/README.md` and `docs/specs/api-contracts.md`
